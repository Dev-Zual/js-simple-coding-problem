const lyrics = "tumi bondhu kala pakhi ami jeno ki.";
const doesExits = lyrics.includes("pakhi");
console.log(doesExits);

const index = lyrics.indexOf("bondhu");
console.log(index);

const start = lyrics.startsWith("umi");
console.log(start);

const end = lyrics.endsWith("ki.");
console.log(end);
