const result = Math.pow(3, 8);
console.log(result);

const num1 = 25;
const num2 = 45;
const gap = Math.abs(num1 - num2);
console.log(gap);
if (gap < 5) {
  console.log("kace aso");
} else {
  console.log("dure jaw");
}

const number = 3.577;
const full = Math.round(number);
console.log(full);

// the number will take up the value
const result2 = Math.ceil(number);
// this will take down the number
const result3 = Math.floor(number);
